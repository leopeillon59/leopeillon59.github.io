---
import FormattedDate from './FormattedDate.astro';

interface Props {
	slug: string;
	title: string;
	description: string;
	pubDate: Date;
	tags: string[];
}

const { slug, title, description, pubDate, tags } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<article
	class="post-card"
	data-post
	data-href={`${base}/blog/${slug}/`}
	data-title={title}
	data-description={description}
	data-tags={tags.join(' ').toLowerCase()}
	role="link"
	tabindex="0"
>
	<h3>{title}</h3>
	<p class="meta">
		<span class="date"><FormattedDate date={pubDate} /></span>
		<span class="tags">
			{
				tags.map((tag) => (
					<button type="button" class="tag" data-tag-filter={tag.toLowerCase()}>
						#{tag}
					</button>
				))
			}
		</span>
	</p>
	<p class="description">{description}</p>
</article>

<style>
	.post-card {
		border: 1px solid var(--border);
		background: var(--surface);
		padding: 1rem;
		display: flex;
		flex-direction: column;
		gap: 0.6rem;
		cursor: pointer;
		transition: border-color 140ms ease, transform 140ms ease;
		border-radius: 4px;
	}
	.post-card:hover {
		border-color: var(--accent);
		transform: translateY(-2px);
	}
	.post-card h3 {
		margin: 0;
		font-size: 1.05rem;
		color: var(--text);
	}
	.meta {
		display: flex;
		gap: 0.75rem;
		align-items: baseline;
		justify-content: space-between;
		margin: 0;
		font-size: 0.85rem;
		color: var(--muted);
	}
	.date {
		font-style: italic;
	}
	.tags {
		display: flex;
		gap: 0.35rem;
		flex-wrap: wrap;
		justify-content: flex-end;
	}
	@media (max-width: 640px) {
		.meta {
			flex-direction: column;
			align-items: flex-start;
			gap: 0.5rem;
		}
		.tags {
			justify-content: flex-start;
		}
		.post-card {
			padding: 0.85rem;
		}
		.post-card h3 {
			font-size: 1rem;
		}
		.description {
			font-size: 0.9rem;
		}
	}
	.tag {
		font: inherit;
		font-size: 0.8rem;
		color: var(--accent);
		background: transparent;
		border: 1px solid var(--border);
		padding: 0.12rem 0.4rem;
		cursor: pointer;
		border-radius: 3px;
	}
	.description {
		margin: 0;
		color: var(--muted);
		font-size: 0.95rem;
		overflow: hidden;
		display: -webkit-box;
		-webkit-line-clamp: 2;
		line-clamp: 2;
		-webkit-box-orient: vertical;
	}
</style>
